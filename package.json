{
  "name": "daily-task-reminder",
  "version": "1.0.0",
  "description": "Daily Task Reminder SPA with Push Notifications",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "web-push": "^3.6.0",
    "@vercel/kv": "^1.0.1"
  }
}
